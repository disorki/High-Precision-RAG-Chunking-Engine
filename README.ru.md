# 🌐 [English](README.md) | Русский

# High-Precision RAG Chunking Engine

Специализированная RAG-система (Retrieval-Augmented Generation) для работы с технической документацией. Включает **умный чанкинг с сохранением таблиц**, **строгое цитирование источников** и **потоковый чат-интерфейс** с контекстной памятью.

![Tech Stack](https://img.shields.io/badge/FastAPI-009688?style=flat&logo=fastapi&logoColor=white)
![Next.js](https://img.shields.io/badge/Next.js-000000?style=flat&logo=next.js&logoColor=white)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-336791?style=flat&logo=postgresql&logoColor=white)
![Ollama](https://img.shields.io/badge/Ollama-000000?style=flat&logoColor=white)

## Ключевые возможности

### Высокоточный RAG
- **Table-Aware Chunking** — сохраняет Markdown-таблицы и структуры данных при разбиении текста.
- **Умный поиск контекста** — фильтрует чанки по порогу релевантности и сортирует их в порядке документа.
- **Строгое цитирование** — каждый факт в ответе сопровождается ссылкой `[Стр. X]`.
- **Переформулировка запросов** — автоматически превращает разговорные вопросы (например, «а что по памяти?») в точные поисковые запросы.

### Продвинутый чат
- **Потоковые ответы** — токены приходят в реальном времени через Server-Sent Events (SSE).
- **Сжатие истории** — автоматически обрезает старые сообщения, чтобы поддерживать бесконечный диалог в пределах контекстного окна.
- **Большое контекстное окно** — оптимизировано для обработки до 16k токенов.

### Технологический стек
- **Бэкенд**: Python 3.11, FastAPI, SQLAlchemy, LangChain.
- **Векторная БД**: PostgreSQL 16 + расширение `pgvector`.
- **LLM-движок**: Ollama (локальный запуск).
  - Эмбеддинги: `nomic-embed-text`
  - Чат-модель: `mistral` (настраивается)
- **Фронтенд**: Next.js 14, TailwindCSS, Lucide Icons.

## Быстрый старт

### Требования
- Docker и Docker Compose
- 8 ГБ+ оперативной памяти (для локального запуска LLM)

### 1. Установка

```bash
# Клонировать репозиторий
git clone https://github.com/disorki/High-Precision-RAG-Chunking-Engine.git
cd "High-Precision RAG Chunking Engine"
```

### 2. Запуск через Docker

```bash
# Собрать и запустить сервисы
docker-compose up --build
```

При первом запуске нужно скачать модели. Откройте отдельный терминал:
```bash
docker exec -it rag-ollama ollama pull nomic-embed-text
docker exec -it rag-ollama ollama pull mistral
```

### 3. Доступ

- **Фронтенд (Чат и загрузка)**: http://localhost:3000
- **API документация**: http://localhost:8000/docs

## Конфигурация (`backend/.env`)

Все параметры RAG-пайплайна и модели настраиваются без изменения кода:

### Основные настройки
| Переменная | По умолчанию | Описание |
|------------|-------------|----------|
| `DATABASE_URL` | `postgresql://...` | Подключение к БД |
| `OLLAMA_BASE_URL` | `http://ollama:11434` | URL Ollama (внутренний) |
| `UPLOAD_DIR` | `/app/uploads` | Путь хранения PDF |

### Настройки RAG
| Переменная | По умолчанию | Описание |
|------------|-------------|----------|
| `CHUNK_SIZE` | `1500` | Размер чанков (больше → таблицы целиком) |
| `CHUNK_OVERLAP` | `300` | Перекрытие чанков (защита от потери смысла) |
| `TOP_K_CHUNKS` | `10` | Количество чанков для поиска |
| `SIMILARITY_THRESHOLD` | `0.3` | Мин. косинусное сходство (0.0–1.0) |

### Параметры чат-модели
| Переменная | По умолчанию | Описание |
|------------|-------------|----------|
| `CHAT_MODEL` | `mistral` | Модель для ответов |
| `CHAT_TEMPERATURE` | `0.3` | Креативность (0.0 = строго, 1.0 = свободно) |
| `CHAT_NUM_CTX` | `16384` | Размер контекстного окна (токены) |
| `CONTEXT_MAX_TOKENS` | `6000` | Макс. токенов для контекста из документа |
| `HISTORY_MAX_CHARS` | `3000` | Лимит символов для истории чата |

## Как пользоваться

1. **Загрузите** PDF-документ (например, техническую инструкцию) на главной странице.
2. **Подождите** обработку — система извлечёт текст, разобьёт на чанки и создаст векторные эмбеддинги (статус: «Processing» → «Ready»).
3. **Задайте вопрос** в чате:
   - Конкретный: *«Какое максимальное напряжение на порту A?»*
   - Общий: *«Перечисли все предупреждения по безопасности»*
   - Система ответит со ссылками на страницы `[Стр. 12]`.

## Структура проекта

```
├── backend/
│   ├── app/
│   │   ├── routers/       # chat.py, upload.py
│   │   ├── services/      # rag_pipeline.py, retrieval.py
│   │   ├── models/        # SQLAlchemy модели БД
│   │   └── config.py      # Pydantic настройки
│   └── Dockerfile
├── frontend/
│   ├── src/
│   │   ├── components/    # ChatInterface, UploadZone
│   │   └── app/           # Next.js страницы
│   └── Dockerfile
└── docker-compose.yml
```

## Лицензия
MIT
