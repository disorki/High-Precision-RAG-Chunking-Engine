version: '3.8'

services:
  # PostgreSQL with pgvector extension
  postgres:
    image: pgvector/pgvector:pg16
    container_name: rag-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ragdb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5

  # Ollama for LLM and Embeddings
  ollama:
    image: ollama/ollama:latest
    container_name: rag-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    # GPU support (requires NVIDIA GPU + NVIDIA Container Toolkit)
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:11434/api/version || exit 0" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Backend FastAPI
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: rag-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/ragdb
      - OLLAMA_BASE_URL=http://ollama:11434
      - UPLOAD_DIR=/app/uploads
      - YANDEX_CLIENT_ID=${YANDEX_CLIENT_ID:-}
      - YANDEX_CLIENT_SECRET=${YANDEX_CLIENT_SECRET:-}
    volumes:
      - ./backend:/app
      - uploads_data:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy
      ollama:
        condition: service_started
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  # Frontend Next.js
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: rag-frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
      - API_URL=http://backend:8000
      - NEXT_PUBLIC_FLOWISE_CHATFLOW_ID=2d705a6e-3943-484a-9489-c79b0e6ce9db
      - FLOWISE_INTERNAL_HOST=http://flowise:3000
      - FLOWISE_API_KEY=jsrW5GQQDIJBSQx_tc9c4B-otQJ5RDzudpGohp2jMiU
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      - backend
    command: npm run dev

  # Flowise — visual AI agent builder
  flowise:
    image: flowiseai/flowise:latest
    container_name: rag-flowise
    ports:
      - "3001:3000"
    environment:
      - DATABASE_PATH=/root/.flowise
      - CORS_ORIGINS=http://localhost:3000,http://localhost:3001
    volumes:
      - flowise_data:/root/.flowise
    depends_on:
      - ollama
      - postgres
    restart: unless-stopped

volumes:
  postgres_data:
  ollama_data:
  uploads_data:
  flowise_data:
